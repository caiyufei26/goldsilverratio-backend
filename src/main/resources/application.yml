server:
  port: 18888
  servlet:
    context-path: /

spring:
  application:
    name: canLite
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/can-lite?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: iFila5408.
    hikari:
      minimum-idle: 2
      maximum-pool-size: 10
      connection-timeout: 30000
      idle-timeout: 600000

mybatis:
  config-location: classpath:mybatis-config.xml
  mapper-locations: classpath:mapper/**/*.xml

app:
  metals: {}
  fred:
    api-key: "743e7fd4a433a831d1f2ce4d69e3a5fa"
  goldapi:
    # 与原先前端写死一致，无需额外配置；可经环境变量 GOLDAPI_TOKEN 覆盖
    token: "${GOLDAPI_TOKEN:goldapi-15ewwosmm6j8wxo-io}"
  jisuapi:
    # 极速数据 appkey，GoldAPI 超限时降级用；可经环境变量 JISUAPI_APPKEY 覆盖
    appkey: "${JISUAPI_APPKEY:1ffce0418678635b}"
